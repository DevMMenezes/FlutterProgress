import 'package:flutter/material.dart';

class TesteWidget extends StatelessWidget {
  const TesteWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('data')),
      body: Text('testadmpppp'),
    );
  }
}
